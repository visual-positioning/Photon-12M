# requirements.txt for your project

# --- PyTorch / core ---
torch>=2.0
torchvision>=0.15

# --- Numerical / images / utils ---
numpy>=1.23
Pillow>=9.0
tqdm>=4.64
opencv-python>=4.7

# --- Transformers / tokenization / acceleration ---
transformers>=4.36
tokenizers>=0.15
accelerate>=0.25

# --- MobileClip (Apple) - installed from GitHub ---
git+https://github.com/apple/ml-mobileclip.git#egg=mobileclip

# --- OpenCLIP (git) ---
git+https://github.com/mlfoundations/open_clip.git#egg=open_clip

# --- COCO / caption evaluation ---
pycocotools>=2.0.7
git+https://github.com/salaniz/pycocoevalcap.git#egg=pycocoevalcap

# --- Metrics / Java wrapper for METEOR ---
torchmetrics>=1.3
pyjnius>=1.6.1

# --- Server / plotting / dev helpers ---
flask>=2.2
matplotlib>=3.7
cython>=0.29  # helps build pycocotools on some systems
